
<view class="page-my-list">
  <view wx:for="{{guidelist}}" wx:key="unique1">
    <navigator url="/pages/guide/detail/detail?id={{item.id}}" class="page-my-list-item" data-id="{{item.id}}">
      <text>{{index+1}}、{{item.name}}</text>
    </navigator>
  </view> 
</view>

