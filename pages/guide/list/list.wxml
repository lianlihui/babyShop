
<view class="page-my-list">
  <view wx:for="{{guizelist}}" wx:key="unique1">
    <view class="page-my-list-item" bindtap="gotoDetail" data-index="{{index}}">
      <text>{{index+1}}、{{item.title}}</text>
    </view>
  </view> 
</view>

