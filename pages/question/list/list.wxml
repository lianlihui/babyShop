
<view class="page-my-list"> 
  <view wx:for="{{guizelist}}" wx:for-item="item" wx:key="unique1">
    <navigator url="/pages/question/detail/detail?id={{item.id}}" class="page-my-list-item" data-index="{{index}}">
      <text>{{index+1}}、{{item.title}}</text>
    </navigator>
  </view> 
</view>

